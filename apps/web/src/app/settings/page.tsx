import { RoutePlaceholder } from '@/components/layout/route-placeholder';

export default function SettingsPage() {
  return (
    <RoutePlaceholder
      title="Settings"
      routePath="/settings"
      description="Settings and profile page placeholder."
    />
  );
}